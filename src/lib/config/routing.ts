// Path exceptions are paths that should not be redirected to the language prefix
const PATH_EXCEPTIONS = ['api', 'sitemap.xml', 'robots.txt', 'favicon.png', 'manifest.json']

export { PATH_EXCEPTIONS }
